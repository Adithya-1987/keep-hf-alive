name: Keep HF Space Alive

on:
  schedule:
    - cron: "*/15 * * * *"

  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Ping Health Endpoint
        run: |
          curl -L -s https://jadhav-1987-alzheimer-mri-api.hf.space/health
